name: Documentation
  
on:
  push:
    branches:
      - master
      - develop
    tags: '*'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: julia-actions/setup-julia@latest
        with:
          version: '1.5.3'
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/FlavellBase.jl.git
          path: ./FlavellBase.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/GPUFilter.jl.git
          path: ./GPUFilter.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/MHDIO.jl.git
          path: ./MHDIO.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/FFTRegGPU.jl.git
          path: ./FlavellBase.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/ImageDataIO.jl.git
          path: ./ImageDataIO.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/WormCurveFinder.jl.git
          path: ./WormCurveFinder.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/Clustering.jl.git
          path: ./Clustering.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/WormFeatureDetector.jl.git
          path: ./WormFeatureDetector.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/SegmentationTools.jl.git
          path: ./SegmentationTools.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/ND2Process.jl.git
          path: ./ND2Process.jl
      - uses: actions/checkout@master
        with:
          repository: flavell-lab/SLURMManager.jl.git
          path: ./SLURMManager.jl
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # For authentication with GitHub Actions token
      - name: Install package
        run: julia --project=docs/ -e 'using Pkg;  Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'
      - name: Build and deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # For authentication with GitHub Actions token
          DOCUMENTER_KEY: ${{ secrets.DOCUMENTER_KEY }} # For authentication with SSH deploy key
        run: julia --project=docs/ docs/make.jl
